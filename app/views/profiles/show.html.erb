<%= link_to profile_info_path(current_user.profile), :class => "fancybox" do %>
	<%= image_tag "help.png", :class => "help-icon" %>
<% end %>
<h2>Mijn gegevens</h2>
<p>
  <b>Naam:</b><br>
  <%= @profile.name %>
</p>
<p>
  <b>Waar ben je goed in?</b><br>
  <%= @profile.expertise %>
</p>

<p>
  <b>Ik kan helpen met...</b><br>
  <%= @profile.willing_to_help_with %>
</p>

<p>
  <b>Ik kan de volgende gereedschappen en materialen inbrengen...</b><br>
  <%= @profile.tools_and_materials %>
</p>

<p>
	<b>Herinneringen:</b><br>
	<% if @profile.send_reminder_mail %>
		- Eén dag voor een toegezegde bijdrage<br>
	<% end %>
	<% if @profile.always_send_project_placement_mail %>
		- Als er een nieuw project wordt gepland<br>
	<% end %>
	<% if @profile.send_project_placement_mail %>
		- Als er een project wordt gepland op een dag dat ik beschikbaar ben<br>
	<% end %>
	<% if !@profile.send_project_placement_mail && !@profile.send_reminder_mail %>
		Geen herinneringen ingesteld
	<% end %>
</p>

<%= link_to 'Profiel bewerken', edit_profile_path(@profile) %>

<h2>Overzicht van mijn bijdragen</h2>
<table class="admin-table table table-bordered table-striped">
	<tr>
		<th>Project</th>
		<th>Bijdrage</th>
		<th>Aantal</th>
		<th>Begindatum</th>
		<th>Einddatum</th>		
		<th>Dagdelen</th>
		<th></th>		
	</tr>
	<% @items.each do |item| %>
		<tr>
			<td><%= item.project.query %></td>
			<td><%= item.name %></td>
			<td><%= get_amount(@profile, item) %></td>
			<td><%= pretty_date(item.start_at) %></td>
			<td><%= pretty_date(item.end_at) %></td>
			<td><%= build_daytext(item) %></td>
			<td><%= link_to "bijdrage intrekken", remove_item_from_profile_path(@profile, item), :confirm => "Weet je zeker dat je deze bijdrage wilt intrekken?" %></td>
		</tr>
	<% end %>	
</table>
