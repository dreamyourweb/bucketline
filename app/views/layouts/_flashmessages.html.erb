<% flash.each do |key, value| %>
	<%= content_tag(:div, :class => "alert alert-block fade in alert-#{key}") do |bla| %>
 		<a class="close" data-dismiss="alert" href="#">×</a>
 		<%= value %>
	<% end %>
<% end %>