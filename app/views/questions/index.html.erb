<div class="row">
	<div class="span4">
		<h2>Handjes</h2>
		<% if user_signed_in? %>
			<%= link_to 'Plaats nieuwe vraag om handjes', :controller => :questions, :action => :new, :type => "help" %>
		<% end %>
		<%= render :partial => "question", :collection => @help %>
	</div>
	<div class="span4">
		<h2>Gereedschappen</h2>
		<% if user_signed_in? %>
			<%= link_to 'Plaats nieuwe vraag om gereedschappen', :controller => :questions, :action => :new, :type => "tool" %>
		<% end %>
		<%= render :partial => "question", :collection => @tools %>
	</div>
	<div class="span4">
		<h2>Materialen</h2>
		<% if user_signed_in? %>
			<%= link_to 'Plaats nieuwe vraag om materiaal', :controller => :questions, :action => :new, :type => "material" %>
		<% end %>
		<%= render :partial => "question", :collection => @materials %>
	</div>
</div>

