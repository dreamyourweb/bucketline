<div class = "well" >
	<% if current_user && item.success %>
		<i class='icon-ok-circle pull-right gift-margin'></i>
	<% end %>
	<% if current_user && item.providing_user(current_user) %>
		<i class="icon-gift pull-right gift-margin"></i>
	<% end %>
	<% if !item.provided %>
		<span class="label label-important pull-right">Nog <%= item.items_left.to_s %> nodig</span>
		<%= render :partial => 'item_info', :locals => {:item => item} %>
		<% if user_signed_in? %>
			<% if item.success %>
				<p>Dit item is al afgevinkt, het is niet meer mogelijk om bij te dragen.</p>
			<% else %>
				<%= form_tag initiative_item_path(initiative, item), :method => "put", :class=>"item-form" do %>			
					<p>Aantal: <%= text_field_tag 'amount_to_give', 1, :class => "amount-input" %> <%= submit_tag "Dit wil ik bijdragen!", :class => "btn" %></p>
					<%= hidden_field_tag :redirect_to_dashboard, true %>
				<% end %>
			<% end %>
		<% else %>
			<p><%= link_to "Log in", login_path %> of <%= link_to "meld je aan", new_user_registration_path %> om bij te dragen</p>
		<% end %>
	<% else %>
		<span class="label label-inverse pull-right">Voorzien</span>
		<%= render :partial => 'item_info', :locals => {:item => item} %>
	<% end %>
	<% if current_user && item.providing_user(current_user) %>
		<%= link_to "Bijdrage intrekken", remove_item_from_profile_path(profile, item)  + "?redirect_to_wishlist=true", :confirm => "Weet je zeker dat je deze bijdrage wilt intrekken?" %>
	<% end %>

	<% if current_user && current_user.initiative_admin(initiative) %>
		<br><%= link_to "Item bewerken", edit_initiative_item_path(initiative, item) %>
		<%= link_to "Item verwijderen", initiative_item_path(initiative, item), :method => :delete, :confirm => "Weet je zeker dat je dit item wilt verwijderen? Alle bijdragen worden dan ook ingetrokken.", :class => "pull-right" %>
	<% end %>
</div>

