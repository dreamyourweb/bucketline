- content_for :header do
  = render :partial => "shared/bucket_group_sub_nav" 

= form_for [@bucket_group, @invitation] do |f|
  .row
    .small-12.large-6.columns
      = f.label :email, "E-mail ontvanger:"
      = f.email_field :email

      = f.submit "Verstuur Uitnodiging", :class => "button"