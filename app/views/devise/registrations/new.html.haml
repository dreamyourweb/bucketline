- content_for :header do
  %h1 Registreer
  %h4.subheader Maak een bijdrage aan een bucket line

.row
  .twelve.columns
    %p Dit registratieformulier is enkel voor losse aanmeldingen. Om bij te dragen aan een Bucket Line moet je via de mail een uitnodiging hebben gekregen. De Bucket Line waardeverbinder zit momenteen nog in de test fase. Als je zelf een Bucket Line wil opzetten en wilt meehelpen met testen, neem dan contact op met info@bucketline.nl 

.row
  .four.columns
    = form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => "form-horizontal"}) do |f|
      = devise_error_messages!
      = f.label "Naam"
      = f.text_field :name
      = f.label :email
      = f.email_field :email
      = f.label "Wachtwoord"
      = f.password_field :password
      = f.label "Wachtwoord bevestigen"
      = f.password_field :password_confirmatio
      = f.submit "Registreren", :class=>"button"
    .login-links
    = render "links"